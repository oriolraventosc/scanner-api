const routes = {
  productsRouter: "/product",
  loadProduct: "/:name",
  scanProduct: "/scan",
  search: "/search",
  login: "/login",
  register: "/register",
  usersRouter: "/user",
};

export default routes;
